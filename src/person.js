export const Person = class {

    constructor(firstName, lastName, gender, height){
    this.firstName = firstName;
    this.lastName = lastName;
    this.gender = gender;
    this.height = height;

    }
}
// constructor constructs an object and gives it several properties. A class is a blueprint

// a universal is the concept from whence an object is built. Objects are instances of that blueprint

//a static class is a class with at least one method declared as static. 